import { z } from "zod";

export default z.object({ "id": z.string().regex(new RegExp("^[a-zA-Z0-9_-]*$")).min(1).max(128).describe("URL safe VTN assigned object ID."), "createdDateTime": z.string().datetime({ offset: true }).describe("datetime in ISO 8601 format"), "modificationDateTime": z.string().datetime({ offset: true }).describe("datetime in ISO 8601 format"), "objectType": z.enum(["PROGRAM","EVENT","REPORT","SUBSCRIPTION","VEN","RESOURCE"]).describe("Types of objects addressable through API.") }).describe("metadata common to all addressable objects. Values provided by VTN on object creation.");
